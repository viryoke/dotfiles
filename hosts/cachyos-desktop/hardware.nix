{
  # GPU and hardware-specific configuration for CachyOS desktop
  # 14900KF + RTX 4060 8GB + 64GB RAM
  # This file is referenced by the system-level NixOS config if applicable,
  # or by chezmoi scripts for hardware-specific setup.
}
